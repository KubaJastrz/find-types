diff --git a/node_modules/gatsby-cli/lib/create-cli.js b/node_modules/gatsby-cli/lib/create-cli.js
index a0684f2..718612c 100644
--- a/node_modules/gatsby-cli/lib/create-cli.js
+++ b/node_modules/gatsby-cli/lib/create-cli.js
@@ -33,6 +33,8 @@ const handlerP = fn => (...args) => {
 
 function buildLocalCommands(cli, isLocalSite) {
   const defaultHost = `localhost`;
+  // remove patch after https://github.com/gatsbyjs/gatsby/pull/20110 is merged
+  const defaultPort = process.env.PORT || `8000`;
   const directory = path.resolve(`.`); // 'not dead' query not available in browserslist used in Gatsby v1
 
   const DEFAULT_BROWSERS = getLocalGatsbyMajorVersion() === 1 ? [`> 1%`, `last 2 versions`, `IE >= 9`] : [`>0.25%`, `not dead`];
@@ -108,8 +110,8 @@ function buildLocalCommands(cli, isLocalSite) {
     }).option(`p`, {
       alias: `port`,
       type: `string`,
-      default: `8000`,
-      describe: `Set port. Defaults to 8000`
+      default: defaultPort,
+      describe: `Set port. Defaults to ${defaultPort}`
     }).option(`o`, {
       alias: `open`,
       type: `boolean`,
